{
    "name": "pokiapp",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "engines": {
        "node": "^20.19.0 || >=22.12.0"
    },
    "scripts": {
        "dev": "cd backend && npm install && cd ../frontend && npm install && cd .. && docker compose -f docker-compose.dev.yml up --build",
        "prod": "docker compose up --build"
    }
}
